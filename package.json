{
  "name": "mad",
  "version": "0.1.0",
  "private": true,
  "description": "Operation M.A.D. — Mobile MDM vendor evaluation platform (iOS & Android)",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=mad-web",
    "build": "npm run build --workspace=mad-web",
    "preview": "npm run preview --workspace=mad-web",
    "server": "cargo run -p mad-server",
    "cli": "cargo run -p mad-cli --",
    "report": "cargo run -p mad-cli -- report --format html --output reports/mad-evaluation-report.html"
  }
}
